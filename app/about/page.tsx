import { AboutPage } from "../components/about/about-page";

export const metadata = {
  title: "About Us — CredLayer",
  description: "Learn about CredLayer's mission to build the trust infrastructure for Web3. Meet our team and discover our vision for a more trustworthy decentralized future.",
};

export default function About() {
  return <AboutPage />;
}
