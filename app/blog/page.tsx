import { BlogPage } from "../components/blog/blog-page";

export const metadata = {
  title: "Blog — CredLayer",
  description: "Stay updated with the latest insights on Web3 trust, blockchain security, AI reputation systems, and decentralized identity verification.",
};

export default function Blog() {
  return <BlogPage />;
}
