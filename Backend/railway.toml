[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn credlayer.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on-failure"
restartPolicyMaxRetries = 10

[env]
ENVIRONMENT = "production"
PYTHONUNBUFFERED = "1"
