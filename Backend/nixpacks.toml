[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r requirements.txt --no-cache-dir"
]

[start]
cmd = "python -m uvicorn credlayer.main:app --host 0.0.0.0 --port $PORT"

[variables]
PYTHONPATH = "/app/src"
PYTHONUNBUFFERED = "1"
